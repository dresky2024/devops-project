name: DevOps CI
on: [push]


jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Run a quick test
        run: echo "Начинаем проверку нашего Docker-проекта..."

      - name: Build Docker Image
        run: docker build -t my-test-image .



